using System;

namespace DeleteTheComments
{
    class Program
    {
        static void Main(string[] args)
        {
            /* Create a function that takes a c# source file path (e.g. "src.cs") as an input, 
            reads the given file, and remove the single line comments from it. It should write the comment-free */
            // code to the original file. (e.g. this line of comment is single lined, the one above not)

            Console.ReadLine();
        }
    }
}

